/* Stub features.h for Nanvix (newlib-based toolchain).
 * Newlib does not provide this header but BusyBox includes it
 * when _NEWLIB_VERSION is defined. Provide an empty stub. */
#ifndef _FEATURES_H
#define _FEATURES_H
#endif
